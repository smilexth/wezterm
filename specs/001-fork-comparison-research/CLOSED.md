# Spec Closed: 001-fork-comparison-research

**Closed**: 2026-08-27
**Status at close**: Research complete, findings conclusive. No further action.

## Why closed

- Purpose was a one-time survey of upstream wezterm branches/forks to decide
  which to adopt. That decision is made; the branch table in `spec.md` is the
  deliverable and remains valid as a reference.
- Nothing here produces ongoing maintenance burden, so "close" means "file
  away", not "abandon".

## What was kept

- `spec.md` — comparison table and per-branch verdicts (reference material).

## Follow-ups spawned

- None. Interesting upstream branches (gamma/gammafix, x11resizecoalesce) were
  already merged upstream by the time of the survey; stale ones (svg, wgpu*,
  boxrender, recording) were rejected as not worth tracking.
